{
  "name": "tombo",
  "version": "0.0.0",
  "main": "scripts/index",
  "devDependencies": {
    "chai": "~1.8.1",
    "component": "~0.17.0",
    "jshint": "~2.3.0",
    "LiveScript": "~1.2.0",
    "mocha": "~1.12.0",
    "uglify-js": "~2.4.3",
    "yuidocjs": "~0.3.46"
  },
  "scripts": {
    "build": "npm run build-component-production",
    "build-component-development": "component-build --dev --standalone tombo --name index",
    "build-component-production": "component-build --standalone tombo --name tombo --out . && uglifyjs tombo.js --output tombo.min.js",
    "build-documentation": "yuidoc ./scripts",   
    "clean": "rm -rf build tombo.js tombo.min.js",
    "clean-deep": "rm -rf components node_modules && npm run clean",
    "install": "component install",
    "lint": "jshint ./scripts",
    "test": "npm run build-component-development && mocha --recursive --compilers ls:LiveScript --reporter dot --timeout 1s --slow 75",
    "validate": "npm test && npm run lint"
  }
}
